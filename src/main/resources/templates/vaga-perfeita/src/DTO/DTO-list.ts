export type Faixa = {
    id: number;
    menorValor: number;
    maiorValor: number;
}

export type Local = {
    id: number;
    nome: string;
}

export type Modalidade = {
    id: number;
    nome: string;
}